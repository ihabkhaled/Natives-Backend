/**
 * Assessment lifecycle states the dashboard signal queries filter on. Kept here
 * so the repository holds only its class and never a raw status literal.
 */
export const ASSESSMENT_PUBLISHED_STATE = 'published';
export const ASSESSMENT_SUBMITTED_STATE = 'submitted';
