# UN-P0-INV release report

**No release performed.**

Both branches (`fix/invitation-membership-linking`) exist locally and have not
been pushed. No deployment ran, no smoke test ran, and no production data was
touched.

| Step                              | State    |
| --------------------------------- | -------- |
| Push                              | Not done |
| GitHub required checks            | Not run  |
| Vercel deployment                 | Not run  |
| Deployed smoke test               | Not run  |
| Production reconciliation dry run | Not run  |
| Production reconciliation apply   | Not run  |

The release procedure to follow when this does ship is in `05-delivery-plan.md`;
the conditions that must be met first are in `22-go-no-go.md`.

This file is deliberately left as a record of "not released" rather than removed
or marked not-applicable. The change is complete and green; it is simply not
live, and that distinction is the thing most easily lost.
