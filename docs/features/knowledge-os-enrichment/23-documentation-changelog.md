# 23 — Documentation Changelog: Knowledge-OS Enrichment & Gap-Fill

## New tooling and data

| Path                                                                            | Purpose                                                               |
| ------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| `tools/knowledge/data/routing-map.mjs`                                          | Authored task→pack map (compiled from `memory/ai-context-map.md`)     |
| `tools/knowledge/data/golden-tasks.mjs`                                         | 12 routing-regression benchmark tasks                                 |
| `tools/knowledge/lib/resolve-pack.mjs`                                          | Pure: task → matched entries → merged curated pack                    |
| `tools/knowledge/lib/run-benchmark.mjs`                                         | Pure: runs golden tasks through the resolver                          |
| `tools/knowledge/lib/run-checks.mjs`                                            | Pure: the contradiction checks (files/mirror-sync/paths/suppressions) |
| `tools/knowledge/lib/load-manifests.mjs`                                        | Shared manifest loader (extracted; re-exported by the resolver)       |
| `tools/knowledge/benchmark.mjs`                                                 | `knowledge:benchmark` CLI                                             |
| `tools/knowledge/verify.mjs`                                                    | `knowledge:verify` CLI                                                |
| `test/tooling/knowledge/{resolve-pack,benchmark,contradiction-checks}.spec.mjs` | 18 new gate tests                                                     |

## New governance documents (Part II gap-fills)

| Path                                      | Gap filled                                                             |
| ----------------------------------------- | ---------------------------------------------------------------------- |
| `skills/resolve-task-context.md`          | §15 — the mandatory-first-step skill for the resolver                  |
| `docs/sdlc/exceptions-register.md`        | §22 — the discoverable index of every sanctioned relaxation + template |
| `runbooks/safe-diagnostics.md`            | §18 — the safe-evidence-collection meta-runbook                        |
| `docs/features/knowledge-os-enrichment/*` | SDLC artifacts (00, 06, 15, 23) for this request                       |

## Updated documents

| Document                                   | Why it changed                                                                              |
| ------------------------------------------ | ------------------------------------------------------------------------------------------- |
| `tools/knowledge/resolve-context.mjs`      | Emits the curated pack; uses the shared `loadManifests`                                     |
| `package.json`                             | Added `knowledge:benchmark` + `knowledge:verify`; appended `knowledge:verify` to `validate` |
| `skills/README.md`                         | Indexed `resolve-task-context.md`; added it as step 0 of the standard agent workflow        |
| `memory/ai-context-map.md`                 | Route row now points at `resolve-task-context.md`                                           |
| `rules/00-non-negotiable-rules.md`         | Rules 4 & 6 now point at the exceptions register                                            |
| `rules/13-eslint-and-typescript.md`        | Strictness-gate intro points at the exceptions register                                     |
| `runbooks/README.md`                       | Now lists all 6 runbooks + the deferred-by-design note                                      |
| `claude.md`                                | "Mandatory Repository Structure" now includes the engineering + compiled-plane folders      |
| `codex.md`, `cursor.md`                    | Synced byte-for-byte with the `claude.md` structure edit (full mirrors)                     |
| `.ai/BOOTSTRAP.md`, `.ai/manifests/*.json` | Regenerated by `knowledge:build` (the new skill + edited docs)                              |

## Deliberately NOT changed (recorded in `06-technical-refinement.md`)

The heavier `knowledge/` + `scripts/knowledge/` compiler; a HOT_MEMORY/active-work file; deployment/secret-rotation/outage runbooks; env-vars.md / local-dev docs; thin skill wrappers; ADR backfill; codex/cursor thinning; §19 AI/ML governance. Each is either owned elsewhere, consciously deferred, or genuinely not applicable — filling them would duplicate or fabricate.

## Remaining documentation gaps

None blocking. The declined items above are recorded with reasoning; they become live work only when their trigger occurs (a deployment target is chosen; an AI/ML component is added; a new relaxation is introduced → the register grows).

## Owners

- Knowledge tooling + gates: repository architect / AI-assisted delivery agent.
- Part II gap-fills + index/route updates: same.
- SDLC artifacts: same.
